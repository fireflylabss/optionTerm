# Implementation Plans

Generated by the `improve` skill on 2026-09-08 for the `optionTerm` repo. These
plans cover the `D1`–`D5` direction findings from the audit at commit `1fa11c6`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Respect GNOME text-scaling-factor in the terminal font | P1 | S | — | DONE |
| 002 | Let users choose which Codex thread to export | P2 | M | — | TODO |
| 003 | Add a context menu to the file tree | P1 | S | — | TODO |
| 004 | Make the command palette run arbitrary typed commands | P2 | S | — | TODO |
| 005 | Add named session profiles (workspaces) | P3 | L | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- These five plans are intentionally independent. They can be executed in any
  order, or in parallel on separate branches.
- If a future plan (outside this set) touches `Session` TOML format, it should
  wait for plan 005.
- If a future UI refactor (e.g. splitting `src/app.rs`) touches the same action
  wiring as plan 003, 004, or 005, that refactor should land first.

## Findings considered and rejected

- None of the bug/perf/security findings from the audit are in this set because
  the user asked for the `D1`–`D5` direction plans only. They can be planned
  separately if needed.
- Quake/dropdown mode and tmux/SSH integration were considered during the
  direction audit but rejected: they require new architecture rather than
  building on the existing adjacent-possible surfaces.
